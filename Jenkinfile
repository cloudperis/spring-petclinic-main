pipeline {
    agent any

    stages {
        stage("SCM"){
            steps {
                sh 'git https://github.com/cloudperis/spring-petclinic-main.git'
            }

        }

        stage("Build"){
            steps {
                echo "build the code"
            }

        }

        stage("Code Quality scan"){
            steps {
                echo "code is scanning"
            }

        }

        stage("Deploy"){
            steps {
                echo "deploy to DEV"
            }

        }
    }
}